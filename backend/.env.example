# Production secret key - keep this very secure and private
SECRET_KEY=""

# Debug mode OFF for production
DEBUG=False

# Allowed hosts for production (your domain names)
ALLOWED_HOSTS=

# Database configuration for production (PostgreSQL example)
DB_ENGINE=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# CORS settings for production
CORS_ALLOWED_ORIGINS=["localhost:3000"]
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOW_HEADERS=[]
CORS_ALLOW_METHODS=[]
